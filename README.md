# Set up

Configuration (craete build folder and Makefile)

```
cmake -S . -B build -G "MinGW Makefiles"
```

# Run

After changes

```
cmake --build build
```

1. Simulations

```
./build/sides_prox_sim.exe
```
